---
title: Sign In Page
description: Sign in page with two-column layout, feature callouts, and branded left panel.
---

<Sandbox id="sign-in-page" shellPreview />

## Usage

```tsx
import { AuthSplitBlock } from "@olwiba/ui";

export default function SignInPage() {
  return <AuthSplitBlock />;
}
```

## Blocks Used

- `MarketingNavBlock` — Minimal top navigation
- `AuthSplitBlock` — Two-column sign-in with branded panel
