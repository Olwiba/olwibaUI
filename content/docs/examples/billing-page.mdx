---
title: Billing Page
description: Billing and subscription management page with plan overview, usage stats, and invoice history.
---

<Sandbox id="billing-page" shellPreview />

## Usage

```tsx
import { StatCard } from "@olwiba/ui";
import { Badge, Button, Card, CardContent, CardHeader, CardTitle, Separator, Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@olwiba/cn";

export default function BillingPage() {
  return (
    <div className="flex min-h-screen">
      {/* Sidebar + main content — see full source in the sandbox */}
    </div>
  );
}
```

## Components Used

- `StatCard` — Usage metric cards with trend indicators
- `Badge`, `Button`, `Card`, `Table` from `@olwiba/cn`
- `Separator` for layout structure
